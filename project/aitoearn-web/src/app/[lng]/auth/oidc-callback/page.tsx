/**
 * OidcCallbackPage - OIDC 登录回调
 * 后台登录成功后跳到这里，凭证在 # 后面
 */

import OidcCallbackContent from './OidcCallbackContent'

export default function OidcCallbackPage() {
  return <OidcCallbackContent />
}
